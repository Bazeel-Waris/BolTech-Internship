import { Component } from "@angular/core";

// It is a Decorator to make this class a component class.
@Component({
    selector: 'app-header',
    templateUrl: './header.component.html',
    styleUrls: ['./header.component.css']
})

// To make this class a component class, we need to decorate it with a Decorator.
export class HeaderComponent { }